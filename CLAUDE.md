# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目状态

这是一个全新的项目仓库，目前尚未初始化任何代码或构建系统。项目名为 **multiagent**，但具体技术栈、框架和架构尚未确定。

### 当前存在的文件

- `.claude/settings.local.json` — Claude Code 本地权限配置
- 无源代码文件
- 无构建配置（package.json、pyproject.toml、go.mod 等）
- 无 README 文档
- 无测试文件或 CI 配置
- 无 Git 历史

## 初始化指导

如果未来需要初始化此项目，请根据所选技术栈执行对应的初始化步骤：

- **Node.js/TypeScript**: `npm init` / `npm create vite@latest` / `npx create-next-app`
- **Python**: `poetry init` / `pip init` / `uv init`
- **Go**: `go mod init`
- **Rust**: `cargo init`
